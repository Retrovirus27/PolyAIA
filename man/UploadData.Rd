% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1_AnnotationSceData.R
\name{UploadData}
\alias{UploadData}
\title{Create an annotated Seurat object}
\usage{
UploadData(
  data,
  DataName,
  Condition = NULL,
  RefAnnotation1 = NULL,
  RefAnnotation2 = NULL,
  Type = c("h5", "Ambient.h5", "counts"),
  RDoublets = TRUE,
  ApplyTags = TRUE,
  IDType = "ENSEMBL",
  ncores = 4
)
}
\arguments{
\item{data}{A raw count matrix or path to an \code{.h5} file.}

\item{DataName}{Character string used to name the resulting Seurat object.}

\item{Condition}{Optional condition/group label attached to the sample.
Default \code{NULL}.}

\item{RefAnnotation1}{First reference dataset for SingleR annotation. Default is \code{NULL}.}

\item{RefAnnotation2}{Second reference dataset for SingleR annotation. Default is \code{NULL}.}

\item{Type}{Input data type. One of \code{"h5"}, \code{"Ambient.h5"}, or \code{"counts"}. Default is \code{"h5"}.}

\item{RDoublets}{Logical. Whether to remove doublets using \code{scDblFinder}. Default is \code{TRUE}.}

\item{ApplyTags}{Logical. Whether to prefix cell barcodes with the sample
tag (\code{DataName}). Default is \code{TRUE}.}

\item{IDType}{Gene identifier type used for annotation (e.g. \code{"ENSEMBL"}).
Default \code{"ENSEMBL"}.}

\item{ncores}{Integer. Number of cores for parallel processing during doublet detection. Default \code{4}}
}
\value{
An annotated Seurat object.
}
\description{
Loads raw count data, removes doublets, and performs cell type annotation
using one or two reference datasets.
}
\examples{
# UploadData(data, DataName = "Sample1", Type = "h5")
# UploadData(data, DataName = "Sample1", RefAnnotation1 = ImmGenData(), RDoublets = TRUE)
}
