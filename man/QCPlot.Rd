% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2_ScePlots.R
\name{QCPlot}
\alias{QCPlot}
\title{Create comprehensive QC plots for Seurat objects}
\usage{
QCPlot(
  seurat_obj,
  count_col = "nCount_RNA",
  feature_col = "nFeature_RNA",
  mito_col = "percent_mito",
  plot_types = "all",
  split_by = NULL,
  count_max = NULL,
  count_breaks = NULL,
  feature_max = NULL,
  feature_breaks = NULL,
  mt_max = NULL,
  mt_breaks = NULL,
  min_counts = NULL,
  max_counts = NULL,
  min_features = NULL,
  max_features = NULL,
  mito_cutoff = NULL,
  bins = 50,
  base_size = 9,
  point_alpha = 0.3,
  n_col = NULL,
  n_row = NULL
)
}
\arguments{
\item{seurat_obj}{Seurat object with QC metrics}

\item{count_col}{Column name for total counts (default "nCount_RNA")}

\item{feature_col}{Column name for features (default "nFeature_RNA")}

\item{mito_col}{Column name for mitochondrial percentage (default "percent_mito")}

\item{plot_types}{Vector of plot types to generate. Options:
"histogram", "violin", "scatter", or "all" (default)}

\item{split_by}{split "histogram", "violin", "scatter" by a variable}

\item{count_max}{Maximum x-axis value for count histogram (auto if NULL)}

\item{count_breaks}{Break interval for count histogram (auto-calculated if NULL)}

\item{feature_max}{Maximum x-axis value for features histogram (auto if NULL)}

\item{feature_breaks}{Break interval for features histogram (auto-calculated if NULL)}

\item{mt_max}{Maximum x-axis value for mt histogram (auto if NULL)}

\item{mt_breaks}{Break interval for mt histogram (auto-calculated if NULL)}

\item{min_counts}{Optional lower filtering cutoff for total counts. If supplied,
drawn as a dashed reference line on the count histogram/scatter/violin plots.
Default NULL (no line drawn)}

\item{max_counts}{Optional upper filtering cutoff for total counts, drawn the
same way as \code{min_counts}. Default NULL (no line drawn)}

\item{min_features}{Optional lower filtering cutoff for detected features,
drawn as a dashed reference line on the feature histogram/scatter/violin
plots. Default NULL (no line drawn)}

\item{max_features}{Optional upper filtering cutoff for detected features,
drawn the same way as \code{min_features}. Default NULL (no line drawn)}

\item{mito_cutoff}{Optional filtering cutoff for mitochondrial percentage,
drawn as a dashed reference line on the mt histogram/violin plots. Default
NULL (no line drawn)}

\item{bins}{Number of bins for histograms (default 50)}

\item{base_size}{Base font size (default 9)}

\item{point_alpha}{Transparency for scatter/violin points (default 0.3)}

\item{n_col}{Number of columns for layout (default NULL, auto-arranged)}

\item{n_row}{Number of rows for layout (default NULL, auto-arranged)}
}
\value{
Combined patchwork plot object
}
\description{
Create comprehensive QC plots for Seurat objects
}
