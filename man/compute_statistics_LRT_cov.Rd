% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/3_PolyAAnalysis.R
\name{compute_statistics_LRT_cov}
\alias{compute_statistics_LRT_cov}
\title{Compute a covariate-adjusted LRT p-value for a proximal–distal polyA site pair}
\usage{
compute_statistics_LRT_cov(
  p_idx,
  d_idx,
  gene_processed,
  trt_covariates,
  ctrl_covariates,
  covariate_formula
)
}
\arguments{
\item{p_idx, d_idx, gene_processed}{As in compute_statistics_LRT()}

\item{trt_covariates}{data.frame, one row per treatment replicate, in the
same order as the treatment columns in gene_processed}

\item{ctrl_covariates}{data.frame, one row per control replicate, in the
same order as the control columns in gene_processed}

\item{covariate_formula}{Character. RHS terms to add, e.g. "Age + Pmi"}
}
\description{
Same as compute_statistics_LRT(), but allows arbitrary per-replicate
covariates to be included in both the full and null models, so that the
likelihood ratio test isolates the condition effect while adjusting for
covariates (e.g., Age, Pmi, batch).
}
\keyword{internal}
