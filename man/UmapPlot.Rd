% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2_ScePlots.R
\name{UmapPlot}
\alias{UmapPlot}
\title{Create UMAP/dimensionality reduction plots}
\usage{
UmapPlot(
  seurat_obj,
  reduction.method = "Harmony",
  color = "Population",
  label_by = NULL,
  split_by = NULL,
  palette_d = "turbo",
  saturation = 0.2,
  alpha_point = 0.5,
  size_point = 0.1,
  palette_c = c("blue", "lightgray", "red"),
  limits = c(-0.1, 1.1),
  breaks = seq(0, 1, 0.2),
  title.prefix = NULL,
  legend.position = "top",
  legend_point_size = 4,
  show_labels = FALSE,
  show_numbers = FALSE,
  n_col = NULL,
  n_row = NULL,
  sizes = c(9, 0.5, 11, 10, 10)
)
}
\arguments{
\item{seurat_obj}{Seurat object}

\item{reduction.method}{Name of the reduction method (e.g., "Harmony", "UMAP", "PCA").
Looked up in \code{seurat_obj} as \code{"umap_<reduction.method>"} (lowercased,
e.g. \code{"umap_harmony"}) since that's this pipeline's convention for the 2D
UMAP embedding, separate from the higher-dimensional integration/PCA space
itself. Falls back to the bare lowercased name if no \code{"umap_"}-prefixed
reduction is found. Coordinate column names are read directly from the
resulting embedding, not guessed from this argument.}

\item{color}{Column name to use for coloring points. Can be continuous or discrete}

\item{label_by}{Column name to use for labels (independent of color). Required if show_labels = TRUE}

\item{split_by}{Optional column name(s) for faceting. Can be a single variable or
a vector of two variables for grid faceting}

\item{palette_d}{Viridis palette option for discrete variables. Default is "turbo"}

\item{saturation}{Desaturation amount for discrete palettes. Default is 0.2}

\item{alpha_point}{Transparency of the points. Default is 0.5}

\item{size_point}{Size of the points. Default is 0.1}

\item{palette_c}{Color palette for continuous variables. A vector of three colors
for low, mid, and high values. Default is c("blue","lightgray","red")}

\item{limits}{Limits for continuous color scale. Default is c(-0.1, 1.1)}

\item{breaks}{Breaks for continuous color scale. Default is seq(0, 1, 0.2)}

\item{title.prefix}{Optional prefix for plot title}

\item{legend.position}{Position of legend. Default is "top"}

\item{legend_point_size}{Size of points in legend for discrete variables. Default is 4}

\item{show_labels}{Logical. If TRUE, adds text labels at the centroid of each group. Default is FALSE}

\item{show_numbers}{Logical. If TRUE, includes cell counts in labels. Default is FALSE}

\item{n_col}{Number of columns for facet layout. Default is NULL (automatic)}

\item{n_row}{Number of rows for facet layout. Default is NULL (automatic)}

\item{sizes}{Numeric value for base font size. Default is c(9,0.5,11,10,10) for the following: base_size, base_line_size, legend.title, legend.text, strip.text, strip.text.x and strip.text.y}
}
\value{
A ggplot2 object
}
\description{
Generate customizable UMAP or other dimensionality reduction plots with
optional faceting, labels, and color schemes for continuous or discrete variables.
}
\concept{visualization}
